#include <iostream> //standard library, basically import from python 

int main(){ 
    //main function, special because it is the starting point of the c++ program, entry point
    //main function exectutes lines in order 

    std::cout << "Number 1" << std::endl; //all lines of code must end with a semi colon
    std::cout << "Number 2"; 

    return 0; 

}
